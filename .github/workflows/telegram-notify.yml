name: Telegram Notify

on:
  issues:
    types: [opened]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send to Telegram
        run: |
          curl -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d "text=📩 *Новое сообщение с сайта!*%0A%0A👤 *Имя:* \${{ github.event.issue.user.login }}%0A📝 *Заголовок:* \${{ github.event.issue.title }}%0A%0A🔗 *Ссылка:* \${{ github.event.issue.html_url }}" \
            -d "parse_mode=Markdown"
